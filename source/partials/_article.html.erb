<article class="<%= class_names.join(' ') unless class_names.nil? %>""">
  <h6 class="sub-header"><%= article.date.strftime('%b %e, %Y') %></h5>
  <h2 class="main-header"><%= link_to article.title, article.url %></h2>
  <%= article.body %>
</article>
